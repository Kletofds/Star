import turtle

myTurtle = turtle.Turtle()
myWin = turtle.Screen()

def zigzag(length):
    if length > 0:
        myTurtle.forward(length)
        myTurtle.right(135)
        myTurtle.forward(length - 1)
        myTurtle.left(90)
        zigzag(length - 1)
    
zigzag(100)
myWin.exitonclick()
